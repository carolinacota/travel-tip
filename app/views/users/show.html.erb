<div class="container">
  <div class="d-flex">
    <div class="text-center align-middle">
      <div class="hidden-form" style="
        position: absolute;
        top: -10000px;
      ">
        <%= simple_form_for(@user) do |f| %>
          <%= f.input :photo, as: :file, input_html: {id: "avatar-input"} %>
          <%= f.submit %>
        <% end %>
      </div>
      <div class="col">
        <% if (current_user == @user && !@user.photo.attached? ) %>
          <p>Upload your pic!<i id="avatar-upload" class="fas fa-upload"></i></p>
        <% elsif @user.photo.attached? %>
          <%= cl_image_tag @user.photo.key, height: 300, width: 300, crop: :fill, :class => "avatar-large", id: "avatar-upload" %>
        <% end %>
        <h1><%= @user.username %></h1>
        <% if (current_user == @user) %>
        <div class="bio-form" style="
          border: none;
        ">
          <%= simple_form_for(@user) do |f| %>
          <%= f.input :bio %>
          <%= f.submit %>
        </div>
        <% end %>
        <% else %>
          <h4>"<%= @user.bio %>"</h4>
        <% end %>
      </div>
      <div class="followers">
        <p class="align-middle"><small>  <i class="fas fa-heart align middle p-1">  </i> 223 followers </small></p>
        <% if current_user != @user %>
        <p class="align-middle"><small>  <i class="fas fa-users align middle p-1">  </i> Follow <%= @user.username%></small></p>
        <% end %>
      </div>
    </div>
  </div>
  <% if current_user == @user %>
    <div class="lists">
      <ul class="nav nav-pills p-2 align middle" style="justify-content: center;" id="pills-tab" role="tablist">
        <li class="nav-item">
          <a class="nav-link active" id="pills-list-tab" data-toggle="pill" href="#pills-home" role="tab" aria-controls="pills-home" aria-selected="true">My Lists</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" id="pills-wishlist-tab" data-toggle="pill" href="#pills-profile" role="tab" aria-controls="pills-profile" aria-selected="false">WishLists</a>
        </li>
      </ul>
      <div class="tab-content" id="pills-tabContent">
        <div class="tab-pane fade show active" id="pills-home" role="tabpanel" aria-labelledby="pills-home-tab">
         <%= render 'lists/list', lists: @lists %>
        </div>
        <div class="tab-pane fade" id="pills-profile" role="tabpanel" aria-labelledby="pills-profile-tab">
          <%= render 'lists/list', lists: @wishlists %>
        </div>
      </div>
    </div>
    <% else %>

      <h4 id="list-header"><%= @user.username %>'s lists</h4>
      <%= render 'lists/list', lists: @lists %>
    <% end %>
</div>

